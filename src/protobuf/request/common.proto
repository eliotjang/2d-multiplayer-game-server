syntax = "proto3";

package common;

message CommonPacket {
  uint32 handlerId = 1;
  string userId = 2;
  string version = 3;
  bytes payload = 4;
}

// Ping 메시지
message Ping {
  int64 timestamp = 1;       // Ping 타임스탬프
}